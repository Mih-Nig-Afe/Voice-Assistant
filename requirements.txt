# Voice Assistant (Miehab) - Dependencies
# Install: pip install -r requirements.txt

# Core dependencies
SpeechRecognition>=3.14.3
pyttsx3>=2.99
requests>=2.32.5
wikipedia>=1.4.0
playsound3>=3.2.8
PyAudio>=0.2.14
psutil>=5.9.8

# AI Backend (Groq — free tier, primary)
groq>=0.18.0
edge-tts>=6.1.12

# AI Backend (HuggingFace — local fallback, optional)
# Uncomment if you want local AI without an API key:
# transformers>=4.36.0
# torch>=2.1.0

# Configuration
python-dotenv>=1.1.1
PyYAML>=6.0.2
fastapi>=0.116.1
uvicorn>=0.35.0

# Development dependencies (install with: pip install -r requirements-dev.txt)
# Or use: pip install -e ".[dev]"
